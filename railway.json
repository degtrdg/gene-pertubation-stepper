{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd api && pip3 install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd api && uvicorn index:app --host 0.0.0.0 --port $PORT"
  }
}
